.flex-1
  %h2
    Editing Actor:
    = @actor.name

  = render 'form'

  %hr.my-2

  = link_to 'Show', [:manage, @actor], class: 'btn action-show'
  = link_to 'Back', manage_actors_path, class: 'btn btn-cancel'

.flex-1
  %h2
    Edit Stage:
    = @stage.name

  = render 'form'

  %hr.my-2

  = link_to 'Show', [:manage, @stage], class: 'btn action-show'
  = link_to 'Back', manage_stages_path, class: 'btn action-cancel'
